name: Build Image

on:
  workflow_dispatch: # 手動でビルドテストを行う

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: |
          docker build -t custom-python-env .
          echo "Build successful."
